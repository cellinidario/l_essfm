# Large regenerable forward signal (877 MB)
forward.npz
results/**/forward.npz
*.npy

# Training logs (TF event files, checkpoints) - keep only the extracted parameters.csv
*_log/
__pycache__/
*.pyc
*.ckpt*
events.out.tfevents.*
forward/

# session scratch (one-shot experiment scripts) and run logs
scratch/
logs/
config/_*.ini
nohup.out
